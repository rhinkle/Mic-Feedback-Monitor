# Mic Feedback Monitor

This is a simple Python application that monitors microphone input and feeds audio back into my headphones so i can hear myself talk without any noticeable delay.

```bash
source .venv/bin/activate
pip install -r requirements.txt
python mic_monitor.py
```
